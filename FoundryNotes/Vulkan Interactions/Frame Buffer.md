A frame buffer is the collection of memory [[Attachment]]s that is used during a [[Render Pass]].

You need at least one Frame Buffer for each [[Image]].

Connections:
- When you create a framebuffer you must pass it an [[Image View]].
- 