Desc:
"Holds Vulkan functions independent of a particular instance"

Connections:
- An entry provides the create_instance() function to create a Vulkan [[Instance]] (Main use of entry)
- Entry is also commonly used as a parameter of other functions
