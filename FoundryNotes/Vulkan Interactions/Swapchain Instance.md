Created by an [[Instance]] for instance-level functions on the [[Swapchain]].
Connections:
- used to create a [[Swapchain]]
-  Used to find all available [[Image]]s in the [[Swapchain]].
- Used to 'acquire' the next available image in the swapchain for rendering to